# ⚠️ AgileFlow Environment Configuration ⚠️
#
# CRITICAL SECURITY INSTRUCTIONS:
# 1. Copy this file: cp .env.example .env
# 2. Replace placeholder values with your actual tokens
# 3. NEVER commit .env to git (it's in .gitignore)
# 4. VERIFY .env is gitignored: grep '\.env' .gitignore
#
# SECURITY WARNING:
# - These tokens grant access to your GitHub, Notion, Context7, and databases
# - If leaked, attackers can read/modify your data
# - Each team member needs their own tokens (NO SHARING)
# - If tokens leak, revoke immediately and regenerate

# =============================================================================
# MCP (Model Context Protocol) Tokens - REQUIRED for /AgileFlow:github-sync and /AgileFlow:notion
# =============================================================================
# These are read by .mcp.json via ${VAR} substitution

# Notion Integration Token
# Get from: https://www.notion.so/my-integrations
# 1. Create a new integration
# 2. Copy the "Internal Integration Token" (starts with "ntn_")
# 3. Share your databases with the integration
# Format: ntn_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
NOTION_TOKEN=ntn_your_integration_token_here

# GitHub Personal Access Token (for MCP)
# Get from: https://github.com/settings/tokens (fine-grained or classic)
# Required permissions: repo (full control), read:org
# Format: ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
GITHUB_PERSONAL_ACCESS_TOKEN=ghp_your_personal_access_token_here

# Context7 API Key (OPTIONAL - for latest framework/library documentation)
# Get from: https://context7.com/dashboard
# IMPORTANT: Context7 works WITHOUT an API key (standard rate limits, public repos)
# API key provides: higher rate limits, private repo access, priority support
# If NOT using: Leave blank or remove this line
# Purpose: Provides Claude with up-to-date docs (overcomes January 2025 training cutoff)
CONTEXT7_API_KEY=your_context7_api_key_here

# =============================================================================
# GitHub Configuration (Legacy/Alternative method)
# =============================================================================
# Note: For MCP, use GITHUB_PERSONAL_ACCESS_TOKEN above
GITHUB_TOKEN=ghp_your_personal_access_token_here
GITHUB_OWNER=your-github-username
GITHUB_REPO=your-repo-name

# =============================================================================
# Supabase (Optional - for database features)
# =============================================================================
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your_anon_key_here
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here
